[project]
name = "template"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.black]
line-length = 127

[dependency-groups]
dev = [
    "pre-commit>=4.1.0",
]
